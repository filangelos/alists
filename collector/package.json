{
  "name": "alists-count",
  "private": true,
  "description": "The collector behind alists. Not part of the site -- the site is static.",
  "scripts": {
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute alists-count --remote --file=./schema.sql",
    "queue": "wrangler d1 execute alists-count --remote --command \"SELECT id, at, cid, name, note, who FROM suggestions ORDER BY id DESC LIMIT 50\"",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
