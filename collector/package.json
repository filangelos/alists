{
  "name": "alists-count",
  "private": true,
  "description": "The collector behind alists. Not part of the site -- the site is static.",
  "scripts": {
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute alists-count --remote --file=./schema.sql",
    "key": "wrangler secret put REVIEW_KEY",
    "queue": "wrangler d1 execute alists-count --remote --command \"SELECT id, name, url, note, who FROM suggestions WHERE state='new' ORDER BY id DESC\"",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
